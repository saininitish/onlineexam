{
  "name": "online-exam-project-root",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "npm install && cd server && npm install && npx tsc -p tsconfig.json",
    "start": "node server/dist/server.js"
  }
}
